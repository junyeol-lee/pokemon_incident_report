<!DOCTYPE html>
<html>
  <head>
    <title>PokemonIncidentReport</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="initial-scale=1.0">
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.8.0/css/bulma.min.css">
    <style>


    </style>
  </head>
    <body>
      <section class="section">
        <div class="container">
          <header>
          <h1 class="title is-1">311 Pokemon service request system</h1>
            <div class="buttons">
              <button class="button is-dark" $button-text-color:$white><%= link_to 'Home', root_path%></button>
              <button class="button is-dark"><%= link_to 'Pokemons', pokemons_path%></button>
              <button class="button is-dark"><%= link_to 'Pokemon nests', pokemonnests_path%></button>
              <button class="button is-dark"><%= link_to 'About Us', permalink_path('about_us')%></button>
            </div>
          </header>
          <%= yield %>
          <footer>
            Pokemon coffee right.
          </footer>
        </div>
      </section>
    </body>
</html>
